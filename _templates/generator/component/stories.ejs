---
to: src/features/<%= feature %>/<%= h.changeCase.pascal(component_name) %>/<%= h.changeCase.pascal(component_name) %>.stories.tsx
---
import type { Meta, StoryObj } from '@storybook/react';
import { <%= h.changeCase.pascal(component_name) %> } from '.';

const meta: Meta<typeof <%= h.changeCase.pascal(component_name) %>> = {
  component: <%= h.changeCase.pascal(component_name) %>,
};
export default meta;

export const Default: StoryObj<typeof <%= h.changeCase.pascal(component_name) %>> = {};
